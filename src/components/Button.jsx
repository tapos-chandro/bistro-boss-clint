

const Button = ({buttonText}) => {
    return (
        <div>
            <button className="btn bg-transparent border-b-4 border-[black] border-0 text-[#1F2937] text-xl ">{buttonText}</button>
        </div>
    );
};

export default Button;